// experimental, we could use this
// the main problem is where the variables representing how many GPUs, CPUs, motherboards in the shop will go
// it would probably be in the shop itself
// you can't use a static variable in the Parts.GPU, CPU, etc. classes because the amount of them is a property of the
// shop, not the item
//import java.util.ArrayList;
//
//public class PartsList extends ArrayList<Part> {
//
//
//}
